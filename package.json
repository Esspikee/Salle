{
  "name": "study-flow",
  "version": "1.0.0",
  "private": true,
  "description": "Private offline university tracker",
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:build:debug": "cap sync android && cd android && gradlew.bat assembleDebug",
    "android:build:release": "cap sync android && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
